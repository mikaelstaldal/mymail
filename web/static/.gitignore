# tsc-compiled output (regenerated by `make -C web compile`)
*.js

# Generated API types (regenerated by `make -C web gen-api`)
/api/types.ts

# Vendored third-party libraries must stay tracked
!/vendor/**
